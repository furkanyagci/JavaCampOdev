package api;

import business.Concrete.UserManager;
import core.Concrete.EmailLogger;
import dataAccess.Concrete.UserDao;
import entities.Concrete.User;

public class Main {

	public static void main(String[] args) {
		User user1 = new User(1, "Furkan", "Yaðcý", "furkanyagci@gmail.com","123456");//@hotmail.com yaparsan false deðer gelir
		UserManager userManager = new UserManager(new UserDao(), new EmailLogger());
		userManager.add(user1);
		
		for (User user : userManager.getAll()) {
			System.out.println(user.getFirstName() + " " + user.getLastName() + " " + user.getEmail() + " " + user.getPassword());
		}
		
		User user2 = new User(2, "Ali", "Yaðcý", "furkanyagci@gmail.com","123456");
		//userManager.add(user2);
	}

}
